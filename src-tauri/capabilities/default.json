{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the CXView desktop workbench",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
